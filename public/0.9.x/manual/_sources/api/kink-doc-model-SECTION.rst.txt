=========================
kink\/doc\/model\/SECTION
=========================

.. contents::
    :local:
    :backlinks: none

.. _api-535710c0f04212088d54dfecfaf987dc:

type section
============

A section of a kinkdoc\.

See “Kinkdoc\: API Documentation system” → “Data model” in the manual\.

.. _api-47eca6d4dda0e4074e3c4fc60b0ee34a:

Section\.title
--------------

\`title\` returns a \`str\` of the title of \`Section\`\.

.. _api-f3985d162a655e34c9380f0ebfb508ac:

Section\.blocks
---------------

\`blocks\` a \`vec\` of the \`block\` values of \`Section\`\.

.. _api-60466cf722d933d8b9a34275e046985f:

Section\.subsections
--------------------

\`subsections\` returns a \`vec\` of \`section\` values which are the subsections of \`Section\`\.

.. _api-786bb6a451f4c3d1978eaded45b29a41:

Section\.to\_json\_val
----------------------

\`to\_json\_val\` returns a JSON value of \`Section\`\.

See “Kinkdoc\: API Documentation system” → “Data model” → “JSON Schema” in the manual\.

.. rubric:: Example

.. code:: kink

    :SECTION.require_from('kink/doc/model/')
    :BLOCK.require_from('kink/doc/model/')
    :BLOCK_TYPE.require_from('kink/doc/model/')
    :JSON.require_from('kink/json/')
        
    :Doc <- SECTION.new(
      'Doc title'
      [ BLOCK.new(BLOCK_TYPE.paragraph 'hey!') ]
      [ SECTION.new(
          'Section title'
          []
          []
        )
      ]
    )
    :Json_val <- Doc.to_json_val
    stdout.print_line(JSON.stringify(Json_val))
    # Output:
    # {
    #   "blocks": [
    #     {
    #       "text": "hey!",
    #       "type": "paragraph"
    #     }
    #   ],
    #   "subsections": [
    #     {
    #       "blocks": [],
    #       "subsections": [],
    #       "title": "Section title"
    #     }
    #   ],
    #   "title": "Doc title"
    # }

.. _api-2b1744b332bf59c5f0e6294be0910d0b:

Section\.depth
--------------

\`depth\` returns an integer \`num\` of the depth of the tree of \`Section\`\.

The depth of a \`section\` is defined as follows\:

\• If the \`section\` has no subsection\, the depth is 1\.

\• If the \`section\` has one or more subsections\, the depth of the \`section\` is the maximum depth of the subsections plus 1\.

.. _api-eb1b9a03f16894a76ddac591b6c6669e:

Section\.limit\_depth\(Max\_depth\)
-----------------------------------

If the depth of \`Section\` is bigger than \`Max\_depth\`\, \`limit\_depth\` returns a \`section\` which is not deeper than \`Max\_depth\`\. The result \`section\` has a same set of subsections as \`Section\`\, except that branches deeper than \`Max\_depth\` are flattened\.

If the depth of \`Section\` is smaller than or equal to \`Max\_depth\`\, \`limit\_depth\` returns \`Section\` itself\.

.. rubric:: Precondition

\`Max\_depth\` must be an integer \`num\` which is bigger than or equal to 2\.

.. rubric:: Postcondition

\`Result\.depth\` is smaller than or equal to \`Max\_depth\`\.

.. _api-e69abb5caf54df1a50c5beb4ca0dcb3d:

X \=\= Y
--------

Two sections are equal when they have equal titles\, equal blocks and equal subsections\.

.. rubric:: Precondition

\`Y\` must be a \`section\`\.

.. _api-e97f5124ca84142c36c9b806f13e949d:

SECTION\.new\(Title Blocks Subsections\)
========================================

\`new\` returns a new \`section\`\.

.. rubric:: Preconditions

\`Title\` must be a nonempty \`str\` which does not start or end with a whitespace character\, and does not contain ASCII control characters\.

\`Blocks\` must be a \`vec\` of \`block\` values\.

\`Subsections\` must be a \`vec\` of \`section\` values\.

.. _api-15acafcda0ba99f6c43556b99b125a7b:

SECTION\.is\?\(Val\)
====================

\`is\?\` returns whether \`Val\` is a \`section\`\.

.. _api-beb66e8f153a42211a28fc26c623ebf0:

SECTION\.from\_json\_val\(Json\_val \.\.\.\[\$config\=\{\}\]\)
==============================================================

\`from\_json\_val\` makes a \`section\` value from \`Json\_val\`\.

Config methods\:

\• C\.on\_success\(\$success\)\: default \= VAL\.identity

\• C\.on\_error\(\$error\)\: default \= \$raise

If \`Json\_val\` matches the JSON Schema of kinkdoc sections\, \`from\_json\_val\` tail\-calls \$success with a \`section\`\.

If \`Json\_val\` does not match the JSON Schema of kinkdoc sections\, \`from\_json\_val\` tail\-calls \$error with a \`str\` of an error message\.

.. rubric:: Preconditions

\`Json\_val\` must be a JSON value\.

.. rubric:: Example

.. code:: kink

    :SECTION.require_from('kink/doc/model/')
    :FLAT_MAP.require_from('kink/container/')
        
    :Json_val <- FLAT_MAP.of(
      'title' 'Greeting program'
      'blocks' [
        FLAT_MAP.of(
          'type' 'paragraph'
          'text' 'This program outputs "hello world" to the standard output.'
        )
        FLAT_MAP.of(
          'type' 'paragraph'
          'text' 'The exit status hall be 0 when it terminates without an error.'
        )
      ]
      'subsections' []
    )
    :Section <- SECTION.from_json_val(Json_val)
    stdout.print_line(Section.repr)
    # => (section title="Greeting program" blocks=[(block type=BLOCK_TYPE.paragraph text="This program outputs \"hello world\" to the standard output.") (block type=BLOCK_TYPE.paragraph text="The exit status hall be 0 when it terminates without an error.")] subsections=[])

